{
  "name": "todolist-server",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cd ./dist && zip -r ./dist.zip ./* && aws lambda update-function-code --region ap-southeast-1 --function-name zcy-todo --zip-file fileb://dist.zip && rm dist.zip && cd .."
  },
  "dependencies": {
    "aws-sdk": "^2.1257.0",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.9",
    "@types/node": "^18.11.9",
    "js-base64": "^3.7.3",
    "typescript": "^4.9.3"
  }
}
